import React from 'react'
import FastImage, {FastImageProps} from 'react-native-fast-image'

const RNImage = (props: FastImageProps) => {
    return <FastImage {...props} />
}

export default RNImage
